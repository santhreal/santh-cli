# BACKLOG - santh-cli
tests/unit/exit_code/maps_to_process_codes.rs:6 | test-gap | high | std::process::ExitCode does not implement PartialEq or Eq, causing standard assertion checks to fail to compile. | Assert on the exit status integers directly or mock the execution of the process rather than comparing ExitCode instances. | status=done | STALE (verified): santh-cli `cargo test` COMPILES and `unit::exit_code::maps_to_process_codes` RUNS GREEN on the current toolchain. std::process::ExitCode implements PartialEq/Debug here, so assert_eq! compiles fine. The claim (ExitCode has no PartialEq -> fails to compile) does not reproduce; no change needed.
tests/benchmark.rs:8 | test-gap | high | benchmark.rs defines a custom fn main() but is treated as an integration test target (harness = true by default) by Cargo, causing E0136 multiple main functions error under cargo test. | Add [[test]] name = "benchmark" harness = false to Cargo.toml or move benchmark.rs outside the tests/ directory. | status=done | STALE (verified): `cargo test --no-run` compiles ALL targets (exit 0) including tests/benchmark.rs which still defines fn main(); the full suite runs green. The claimed E0136 multiple-main does not reproduce on the current cargo/toolchain; no change needed.
tests/adversarial/cli/duplicate_config_rejected.rs:15 | test-gap | high | The test expects duplicate --config flags to trigger an ArgumentConflict clap error, but by default clap v4 overrides the value and parses successfully, causing the test to fail. | Add conflicts_with = "config" or similar override prevention to the config field in GlobalFlags, or fix the test expectation. | status=done | STALE (verified): the adversarial CLI suite runs GREEN, so duplicate_config_rejected passes as-is; the claimed clap-override failure does not reproduce. No change needed.
src/runner.rs:105 | capability | high | The Ctrl+C interruption flag is a local Arc<AtomicBool> in run_inner and is not exposed via any public API. Long-running scanner implementations of SanthCli::run have no way to poll or check if an interruption was requested, preventing them from aborting early. | Expose a public function (e.g. santh_cli::is_interrupted() -> bool) backed by a global atomic instead of a local one. | status=done | FIXED: replaced the local Arc<AtomicBool> interrupt flag with a process-global `static INTERRUPTED: AtomicBool` and a public `santh_cli::is_interrupted() -> bool` (re-exported from lib.rs). Global is the honest model because ctrlc::set_handler is itself process-wide. Long-running SanthCli::run impls can now poll is_interrupted() to abort early. install_ctrlc_handler() sets the global; run_inner checks is_interrupted(). Proving test gap::interrupt_flag_is_pollable. Suite green.
src/runner.rs:232 | bug | medium | The tracing logging initialization hardcodes "santh-cli" as the tool name instead of using T::tool_name() from SanthCli, clobbering the correct name in log spans and metrics. | Pass T::tool_name() into init_logging inside run_inner. | status=done | FIXED: init_logging now takes a tool_name param and run_inner passes T::tool_name() instead of the hardcoded "santh-cli", so log spans/metrics carry the real tool name. Suite green.
src/finding.rs:59 | perf | low | Needless allocation of intermediate Vec<String> and string copies via iter().map(ToString::to_string).collect() for tags, cveIds, cweIds, references, and matchedValues when the slices could be serialized directly by the json! macro. | Pass the slices directly to the json! macro without mapping to String. | status=done | FIXED (Law-7): the accessors return `&[Arc<str>]` and serde serializes Arc<str> directly, so replaced all five `finding.X().iter().map(ToString::to_string).collect::<Vec<_>>()` with `finding.X()` in the SARIF json! block. Drops the 5 intermediate Vec<String> allocations per emitted finding (the per-element to_string copies as well; the final Value::String owning is inherent to serde_json::Value). NOTE: the finding-value part of the row was slightly overstated - Value::String must own its String so the array-element allocation into Value is not eliminated, only the explicit Vec<String> collect is; net a real but modest win + cleaner code. Also STRENGTHENED the weak existing test: finding_output.rs only asserted is_ok()+!is_empty() (a banned tautology per the Testing Contract). Added sarif_properties_arrays_serialize_string_values which builds a finding with tags/cve/cwe/reference/matched_value populated, emits SARIF, parses it, and asserts each properties.* array equals the exact expected JSON string array (proving the optimization is byte-equivalent output, incl. that the builder stores tags sorted and matchedValues preserves exact bytes like "; rm -rf /"). Suite green (10 test binaries, 0 warnings).
src/finding.rs:38 | santh-cli | silent-fallback | medium | REPOPULATE 2026-07-16: location.line and location.column are None, substituting 1, which incorrectly attributes file-level findings to line 1 column 1 in downstream SARIF consumers. | Omit region or startLine/startColumn from SARIF output when the finding location line/column are None + a proving test in tests/integration/finding_output.rs asserting that a finding with a None location line produces a SARIF document with no region block or no startLine field (instead of defaulting to 1). | FIXED (Law-10): emit_sarif no longer fabricates `startLine`/`startColumn` = 1. The `region` block is added only when `location.line` is `Some` (built by mutating the `physicalLocation` Value), and `startColumn` only when `location.column` is `Some` alongside a line. A file-level finding now emits `physicalLocation` with just `artifactLocation` and no `region`. Proving tests tests/integration/finding_output.rs::sarif_omits_region_when_finding_has_no_line (asserts `region` is null / absent) and sarif_emits_real_line_and_column_when_present (asserts startLine==42, startColumn==7 from real data, not 1). Gate: cargo test integration binary 4 passed, EXIT=0. | status=done
src/config.rs:20 | santh-cli | silent-fallback | low | REPOPULATE 2026-07-16: candidate.is_file() returns false on access metadata errors, silently falling back to T::default() instead of surfacing the read/permission error. | Surface non-NotFound I/O errors as ConfigRead + a proving test asserting that resolve_config with a default path that exists but cannot be accessed returns Err(SanthError::ConfigRead). | FIXED (Law-10): replaced `candidate.is_file()` (which collapses every error to false) with an explicit `std::fs::metadata` match: `Ok(is_file)` → load; `Ok(other)` → skip; `Err(NotFound)` → try next / default; any other `Err` → return `SanthError::ConfigRead { path, source }`. A permission/ENOTDIR error on a default path now surfaces instead of silently defaulting. Proving tests config::tests::resolve_config_surfaces_non_notfound_io_error_on_default_path (a regular file used as a directory component → ENOTDIR → ConfigRead; portable, no root/permission setup) and resolve_config_defaults_when_default_path_is_absent (NotFound still defaults). Gate: cargo test config::tests 2 passed, EXIT=0. | status=done